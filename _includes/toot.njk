<section class="toot">
	<header>
		<span><a href="{{ toot.actor }}">{{ toot.actor }}</a></span>
		<span>{{ toot.published }}</span>
	</header>
	{# Render Toot #}
	{% if (toot.type == "Create") %}
		{% include 'toot/Create.njk' %}
	{# Render Boost #}
	{% elif (toot.type == "Announce") %}
		{% include 'toot/Announce.njk' %}
	{% endif %}
</section>